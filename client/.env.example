# URL base de la API
# Desarrollo: /api  (usa el proxy de Vite, apunta a localhost:4000)
# Produccion: https://tu-backend.onrender.com/api
VITE_API_URL=/api

# Google OAuth Client ID (el mismo que en el backend)
# console.cloud.google.com > Credenciales > OAuth 2.0
VITE_GOOGLE_CLIENT_ID=
